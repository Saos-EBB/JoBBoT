## 2026-07-31 — feat(ui): quadrate erscheinen grau, färben sich erst am ende der pop-animation ein

**Was:** Kevin: "mach die quadrate beim erscheinen grau und wenn ... durch ist mach die die passen so blau und den rest gelb". `.loadgrid__sq--done`/`--error` nutzen jetzt eigene Keyframes (`loadgrid-pop-done`/`-error`) statt fixer `background`-Werte: 0–55% grau (`var(--line)`, wie der alte "noch leer"-Zustand aus Step 2), ab 100% blau (`#5B8CFF`, "passt" — Filter: Sicher; Scrape: alle gezeigten Quadrate, da nur Kandidaten nach dem Location-Gate überhaupt Quadrate werden) bzw. gelb (`#E8B04B`, "Rest" — Filter: Unsicher/Raus; Anschreiben: fehlgeschlagen, ersetzt das vorherige Rot).
**Nicht gebaut:** Keine eigenen "abgelehnt vom Location-Gate"-Quadrate für Scrape — nur Kandidaten, die die Gate schon bestanden haben, werden überhaupt als Quadrat gezeigt (unverändert seit Step 4), die Rest-Farbe kommt dort aktuell also nie vor.

## 2026-07-31 — feat(ui): quadrate erscheinen einzeln gestaffelt statt zeilenweise auf einen Schlag

**Was:** Kevin: "das pro Zeile sieht nicht gut aus" — widerspricht dem ursprünglichen YAGNI-Gate im Auftrag ("kein Einzel-Quadrat-Füll-Effekt"), aber auf Wunsch umgesetzt. Rein CSS: `.loadgrid__sq` startet unsichtbar/verkleinert und poppt per `@keyframes loadgrid-pop` (Opacity+Scale) ein, mit `animation-delay` pro Quadrat-Index innerhalb seiner Zeile (`ui/app.tsx` LoadGrid). Die Daten selbst kommen weiterhin als komplette Zeile in einem SSE-Event (keine Änderung an Scrape/Filter/Anschreiben-Anbindung nötig) — nur die Darstellung staffelt sich.
**Nachtrag 1:** Erste Version (45ms Versatz, 220ms Dauer) war laut Kevin "way too fast"/nicht sichtbar — auf 150ms Versatz, 350ms Dauer verlangsamt (10er-Zeile jetzt ~1,7s statt ~0,65s).
**Nachtrag 2:** Kevin: "mach eine zeile auf 4 sek". Fixer Versatz ersetzt durch `ROW_DURATION_MS / squares.length` — 10er-Zeile (Scrape/Filter) füllt sich über ~4s, 1er-Zeile (Anschreiben) erscheint sofort (Versatz bei Index 0 immer 0).
**Nachtrag 3 — Ursache gefunden:** Animation lief bei Kevin nie, trotz aller Timing-Anpassungen — Ursache war die eigene Vorsichtsmaßnahme: die `@media (prefers-reduced-motion:reduce)`-Regel aus Nachtrag 1 hatte die Animation komplett abgeschaltet, weil Kevins GNOME Animationen systemweit deaktiviert hat (`gsettings get org.gnome.desktop.a11y.interface enable-animations` → `false`, mappt im Browser auf `prefers-reduced-motion: reduce`). Regel entfernt — Kevin will die Animation explizit, unabhängig von der Systemeinstellung.
**Nicht gebaut:** Kein Fortschritt bei echten Item-Fertigstellungen selbst (die Datenquelle liefert weiter ganze Zeilen auf einmal) — nur eine visuelle Staffelung des bereits vorhandenen Zeilen-Events. Kein `prefers-reduced-motion`-Support mehr (siehe Nachtrag 3, bewusst entfernt).

## 2026-07-31 — feat(ui): lade-grid bleibt nach abschluss sichtbar, bis manuell geschlossen

**Was:** Kevin: "verschwindet zu schnell" — das Grid wechselte bisher exakt in dem Moment, in dem der Lauf auf "done" kippte, zurück zur Auswahl-Form; nichts blieb zum Nachschauen stehen. Neue geteilte `LoadGridPanel`-Komponente (`ui/app.tsx`, neben `LoadGrid`) für alle drei Ansichten: Anzeigebedingung ist jetzt `running || sections.length > 0` statt nur `running` — das Grid bleibt also nach Abschluss stehen. Ein "Schließen"-Button (nur sichtbar wenn nicht mehr running) leert die jeweilige `sections`-State und schaltet damit zurück zur Auswahl-Form. Ein neuer Lauf leert `sections` ohnehin beim Start (`runScrapeNow`/`runFilterNow`/`runAnschreibenNow`, seit den vorherigen Steps), ersetzt das alte Grid also automatisch. Verifiziert per Playwright gegen einen echten Live-Scrape (karriere.at): Grid blieb nach "Fertig" mit sichtbarem "Schließen"-Button stehen, Klick darauf brachte die Quellen-Auswahl zurück.
**Nicht gebaut:** Kein Timeout/Auto-Ausblenden nach X Sekunden — bewusst rein manuell, wie gewünscht.

## 2026-07-31 — chore(ui): alte progressbar-anzeige durch lade-grid abgelöst

**Was:** In allen drei `dt__body`-Ansichten (Scrape/Filter/Anschreiben) die alten Text-/Balken-Anzeigen entfernt, die neben dem Grid noch liefen: Scrapes per-Quelle-`current/total`-Textliste, Filters "i/total: Titel"-Zeile, Anschreibens Phasen-Text ("Prompt bauen…/Generieren…/Fertig") samt Shimmer-Bar und Current-Zeile. Toter `anschreibenPhase`-Const (seit Step 3 immer `null`) und die jetzt ungenutzte `.fld__bar--live`-CSS-Regel samt `@keyframes fld-shimmer` gelöscht. Übrig bleibt je Ansicht nur "Läuft…" + Grid (oder "Startet…" bis die erste Zeile da ist). Geprüft: Typecheck, 250 Tests, Playwright-Screenshot aller drei Ansichten im Leerlauf-Zustand — rendert fehlerfrei.
**Nicht gebaut:** Die Sidebar-Mini-Balken unter den Nav-Buttons (`.fld__bar` mit `scrapePct`/`filterPct`/`anschreibenPct`) bleiben unangetastet — reine "läuft gerade"-Indikatoren ohne Item-Bezug, kein Platz für ein Grid in der schmalen Sidebar, waren nie Teil der "Lade-Anzeige, dort wo sie lief" (das ist der Hauptbereich). Eine bewusste Abgrenzung, nicht im Auftrag einzeln abgefragt, weil sie nur diese eine Datei betrifft.

## 2026-07-31 — feat(ui): filter-vorgang im lade-grid

**Was:** Filter bekommt entgegen der ursprünglichen Auftrags-Vermutung eine volle UI-Anbindung (Recon Step 1 bestätigte das) — also nicht übersprungen. Drei `createBatcher`-Instanzen in `scripts/ui-server.ts` (eine je Ergebnis-Kategorie: Sicher/Unsicher/Raus statt der im Auftrag genannten zwei "Match/Aussortiert" — der echte Filter-Code liefert drei Ausgänge, `matched`/`uncertain`/`filtered_out`, siehe `lib/filter-decide.ts`; die dritte Kategorie zu unterschlagen wäre ungenau gewesen), je 10 Jobs pro Zeile, über den neuen `filterSse`-Kanal (`/api/filter/stream`, gleiches Muster wie Scrape). Tooltip: Titel — Firma — Ergebnis, kein Aussortier-Grund (YAGNI-Gate im Auftrag). Client: `<LoadGrid sections={filterSections}/>` neben der alten current/total-Zeile. Verifiziert mit einem Wegwerf-Skript gegen `filterJob()` im Regex-Modus (drei Test-Jobs, echte Klassifikation) — Kategorisierung und Batching bestätigt, ohne echte `data/jobs`-Daten anzufassen (aktuell 0 Jobs mit `status:"new"`, ein Live-Lauf über die UI hätte nichts gezeigt).
**Nicht gebaut:** kein Aussortier-Grund im Tooltip (siehe Auftrag). Kein Fehler-Quadrat — ein Filter-Fehler bricht den ganzen Lauf ab (bestehendes Verhalten), es gibt keinen Pro-Job-Fehlerfall.

## 2026-07-31 — feat(ui): anschreiben-generierung im lade-grid mit hover-infos

**Was:** `<LoadGrid sections={anschreibenSections}/>` rendert jetzt in der Anschreiben-Ansicht (`ui/app.tsx`), gespeist von der in Step 3 gebauten SSE-Anbindung. Serverseitig war die eigentliche Arbeit schon in Step 3 erledigt (SSE-Payload + `onItemDone` in `lib/anschreiben-runner.ts`) — dieser Schritt ist die sichtbare Verdrahtung. Verifiziert mit einem Wegwerf-Skript gegen einen Mock-Ollama-Server (kein 3-4-Minuten-Live-Lauf nötig, siehe `test/anschreiben.test.ts`s eigenes `mockChat`-Muster): Erfolgsfall liefert `{tooltip: "Junior Dev — Test GmbH — mistral-small3.2:latest — ~41 Wörter — gespeichert", state: "done"}`, Fehlerfall (Parse-Fehler bei zu kurzer Antwort) liefert `{tooltip: "... — fehlgeschlagen", state: "error"}` — beide Zustände des Grids bestätigt.
**Nicht gebaut:** —

## 2026-07-31 — feat(ui): scrape-fortschritt im lade-grid mit hover-infos

**Was:** `ScraperAdapter.scrape()` bekommt einen vierten optionalen Callback `onUnitDone?: (items: ScrapedJob[]) => void` (`scrapers/interface.ts`) — feuert pro abgeschlossener Grid-Zeile. Bei echter Suchergebnis-Pagination (`devjobs-at.ts`, `ams.ts`, und — beim Implementieren korrigiert — auch `linkedin.ts`, das mit `PAGES=[0,25,50]` ebenfalls echte Seiten abruft, entgegen meiner ursprünglichen Einordnung als "keine Pagination") ist die Zeile eine Suchseite, Titel/Firma/Ort kommen direkt aus dem Parse. Bei den zwei verbleibenden Quellen ohne echte Pagination (`karriere-at.ts`, `jobs-at.ts`) gruppiert `lib/grid-batch.ts` (`createBatcher`, neu, von beiden geteilt) den Job-für-Job-Detail-Abruf in feste 10er-Blöcke. `lib/scrape-runner.ts` reicht `onUnitDone` mit Quellenname durch; `scripts/ui-server.ts` baut daraus `GridUnitEvent`s über den in Step 3 gebauten `scrapeSse`-Kanal (`/api/scrape/stream`), ein Row-Zähler pro Quelle sorgt für eindeutige Keys. Client: `<LoadGrid>` rendert in der Scrape-Ansicht neben der alten current/total-Textliste (die bleibt bis Step 7). Live gegen karriere.at getestet (echter Netzwerk-Scrape, 17 Treffer korrekt in 10+7 gebatcht, Tooltips zeigen reale Titel/Firma/Ort) — Test-Artefakt (1 gespeicherter Job) danach wieder gelöscht.
**Nicht gebaut:** kein Fehler-Quadrat für Scrape — ein gescheiterter Detail-Fetch liefert weiterhin den Basis-Job (siehe bestehendes Verhalten), eine gescheiterte Suchseite hat keine Job-Identität für ein sinnvolles Fehler-Quadrat und wird einfach ausgelassen (keine Zeile für diese Seite). Kein Reconnect-Snapshot (wie Anschreiben-SSE, Step 3).

## 2026-07-31 — refactor(ui): sse-nutzlast von fortschritt auf einheit-fertig+items

**Was:** Die bisherige Anschreiben-SSE (`{phase: 'prompt'|'generating'|'done'}`, `scripts/ui-server.ts`) trug reinen Ollama-Streaming-Fortschritt. Jetzt trägt sie `GridUnitEvent` (`{section, sectionLabel, row, items}`) — ein Event pro abgeschlossenem Grid-Item (Anschreiben: 1 Item/Zeile, da jede Generierung Minuten dauert und eine 10er-Batch-Regel wie bei Filter lange Stille bedeutet hätte, siehe Klärung mit Kevin). `createSseChannel<T>()` ersetzt den anschreiben-spezifischen Client-Set+Broadcast-Code — dieselbe generische Funktion bedient künftig auch Scrape/Filter (Steps 4/6), keine drei Kopien. `lib/anschreiben.ts`/`lib/anschreiben-runner.ts`: `onPhase` (Prompt/Generating/Done-Ticks, auch pro NDJSON-Chunk) komplett entfernt, `onItemDone` liefert stattdessen Titel/Firma/Modell/grobe Wortzahl/gespeichert bzw. "fehlgeschlagen" nach Abschluss eines Jobs. Client sammelt die Events in `anschreibenSections` (State), noch nicht gerendert — das folgt in Step 5.
**Nicht gebaut:** kein Reconnect-Snapshot (wer mittendrin verbindet, sieht nur ab da — Einzelnutzer-Lokaltool, gleiche Toleranz wie beim bestehenden Server-Neustart-Verlust). `anschreibenPhase`/die alte Balken-JSX bleiben unverändert liegen (jetzt tot, da nichts mehr `phase` sendet) — Löschen ist Step 7, nicht hier, sonst kompiliert der noch unveränderte Anschreiben-View-Block nicht.

## 2026-07-31 — feat(ui): einheitliche lade-grid-komponente (abschnitt/zeile/quadrate)

**Was:** `LoadGrid` in `ui/app.tsx` — eine gemeinsame Render-Funktion für alle drei Lade-Anzeigen (Scrape/Filter/Anschreiben), statt drei eigener Implementierungen. Nimmt `LoadGridSection[]` (Abschnitt -> Zeile -> Quadrate); jedes Quadrat ist `done` oder `error`, Hover-Tooltip über natives `title`-Attribut (kein eigenes Tooltip-Bauteil nötig). Mit Dummy-Daten visuell geprüft (Playwright-Screenshot), noch nirgends verdrahtet — das folgt in den nächsten Schritten zusammen mit der SSE-Nutzlast.
**Nicht gebaut:** kein `pending`-Zustand (nur bei Scrape wäre die Gesamtzahl vorab unbekannt gewesen, siehe Recon — Zeile für Zeile nachwachsen reicht für alle drei), keine eigene Tooltip-Komponente (Browser-natives `title` reicht für eine Kurzinfo), keine Sortier-/Filter-Interaktion auf dem Grid.

## 2026-07-17 — chore(config): disable ams source, blocked by anti-bot token + robots.txt

**Was:** `npm run scrape:ams` scheiterte (auch standalone, nicht UI-spezifisch) mit `Timeout ... waiting for event "response"` in allen drei Queries. Debug-Playwright-Run zeigt: AMS liefert für `/api/statusinfo`, `/api/frontend/aem-content`, `/api/frontend/predefined-searches` und `/api/search` jetzt `401 Unauthorized` — jeder Request braucht einen client-seitig signierten `x-ams-access-token`-Header (Algorithmus steckt im minifizierten `main.js`-Bundle), den unser Scraper nicht mitliefert. Zusätzlich verbietet `robots.txt` explizit `Disallow: /public/emps/` für alle User-Agents außer `LinkedInBot` — das ist keine zufällige Frontend-Änderung, sondern eine bewusste Sperre gegen automatisierten Zugriff. Token-Mechanismus wird deshalb nicht nachgebaut (würde robots.txt + Anti-Bot-Schutz gleichzeitig umgehen). `ams` in `config/sources.json` auf `enabled: false`, `scrapers/ams.ts` bleibt unverändert liegen für den Fall, dass AMS den Zugriff wieder öffnet.
**Nicht gebaut:** kein Ersatz-Scraper für AMS-exklusive Inserate — laut AMS speist sich `jobs.ams.at` aus eJob-Room-Selbstbedienung, direkt gemeldeten Firmenanforderungen, aus dem Web aggregierten Firmen-/Behörden-Inseraten und teils sogar Inseraten der deutschen Bundesagentur für Arbeit. Kein 1:1-Ersatzkanal, also nichts Neues gebaut, nur recherchiert.

## 2026-07-17 — feat(storage): give brutal-fit jobs their own subfolder
**Was:** Nachtrag zum vorherigen Schritt — `brutal` blieb im Basisordner statt wie `matched`/`offstack` einen eigenen Unterordner zu bekommen (Kevin: "brutal fehlt noch für die aussortierten"). `JsonStore.dirFor()` routet jetzt alle drei fit-Werte, `allDirs()` kennt `data/jobs/brutal/`. Tests (`save(fit=brutal)`, erweiterter `list()`-Test über alle drei Unterordner) und die Storage-Absätze in `AGENTS.md`/beiden READMEs ergänzt. Die `--data=save`/`--data=unsave`-Zeilen in den READMEs bleiben unverändert — die betreffen nur die Anschreiben-CLI, die brutal-Jobs ohnehin nie einschließt (`fit !== 'brutal'`).
**Nicht gebaut:** —

## 2026-07-17 — refactor(storage): rename sicher/unsicher dirs to matched/offstack
**Was:** Letzter Rest der alten Vokabular-Mischung — die physischen Ordnernamen `data/jobs/sicher/`/`unsicher/` waren noch auf Deutsch benannt, während `fit` längst `matched`/`offstack` heißt. Ordner umbenannt (waren leer, reines `mv`), `JsonStore` folgt: `FIT_DIRS`-Lookup-Map komplett entfernt, da Ordnername jetzt 1:1 dem fit-Wert entspricht (`join(this.dir, job.fit)` statt Übersetzungstabelle). `test/storage.test.ts`, `AGENTS.md`, `README.md`, `README.en.md`, `scripts/anschreiben-model-bench.ts`, `scripts/run-anschreiben.ts` nachgezogen. `lib/filter-llm.ts`s `'unsicher'` (LLM-Kriterien-Antwort `Tri`) und die `Decision.status`-Domäne bewusst NICHT angefasst — andere Vokabular-Ebene, siehe vorheriger Schritt. Die beiden Erwähnungen von `sicher/unsicher` im vorherigen build-log-Eintrag bleiben unverändert (Log, keine Live-Doku).
**Nicht gebaut:** —

## 2026-07-17 — chore(model): remove dead job.match field
**Was:** Nachtrag zum vorherigen Schritt (auf Wunsch erst NACH der Vereinheitlichung geprüft). `Job.match` (`{ok, reason} | null`) wurde nur je auf `null` initialisiert (`lib/normalize.ts`) und nirgends sonst beschrieben — die einzige Leseseite (`scripts/ui-server.ts`, Legacy-HTML-View) konnte den truthy-Zweig nie erreichen. Feld aus `scrapers/interface.ts`, `lib/normalize.ts` und dem toten Render-Zweig entfernt; zugehöriger Test (`match === null`) gelöscht. Datenmigration entfällt — `data/jobs` ist aktuell komplett leer (0 Dateien in allen Unterordnern), nichts trägt den alten Wert.
**Nicht gebaut:** —

## 2026-07-17 — refactor(model): unify fit/status vocabulary
**Was:** `Job.status` und `Job.fit` trugen dasselbe Urteil in zwei verschiedenen Wörtern (`status: 'matched'/'uncertain'/'filtered_out'` vs. `fit: 'match'/'offstack'/'brutal'`) — Kevin hatte das bei einer früheren Kopplung (Commit `5f932c9`) benannt, aber nicht konsolidiert. Jetzt: `JobStatus` verliert die drei Triage-Werte zugunsten eines einzigen `'triaged'`; das Urteil selbst lebt nur noch in `Fit = 'matched' | 'offstack' | 'brutal'` (umbenannt von `'match'`). Physisches Ordner-Routing (`storage/json-store.ts`, sicher/unsicher) und UI-Ordner-Routing (`lib/folders.ts`, jobs/aussortiert) laufen jetzt über `fit` statt `status`. Die `Decision`-Schicht (`lib/filter-decide.ts`, `filter-llm.ts`, `filter-regex.ts`, `filter-types.ts`) bleibt unangetastet — deren `matched`/`uncertain`/`filtered_out` ist ein eigenes, in sich stimmiges Vokabular der Filter-Strategie, nicht dasselbe Feld wie `Job.status`, und wird von `lib/filter.ts` in `fit` übersetzt. Zwei jetzt obsolete One-off-Migrationsskripte (`migrate-fit-from-status.ts`, `migrate-status-fit.ts`, beide auf altem Schema) gelöscht — ersetzt durch eine neue Datenmigration im nächsten Schritt.
**Nicht gebaut:** kein neues Storage-Filter-Feature für `fit` (z.B. `list({fit})`) — `scripts/run-anschreiben.ts` filtert stattdessen in-memory nach dem `list({status:'triaged'})`-Ergebnis, kein Interface-Wachstum für einen einzigen Call-Site.

## 2026-07-17 — chore(data): clear data/jobs/sicher and unsicher (intentional, per Kevin)
**Was:** 145 Job-JSONs aus `data/jobs/sicher/` und `data/jobs/unsicher/` entfernt (144 Deletes + 1 zuvor modifizierte Datei). Verursacht durch mehrere parallel laufende `ui-server.ts`-Prozesse (3 gleichzeitig, seit 11:23 bzw. 11:49) — auf Nachfrage bestätigt: gewollt, nicht versehentlich. Zwei überzählige Prozesspaare beendet, ein sauberer Lauf bleibt aktiv.
**Nicht gebaut:** —

## 2026-07-17 — refactor(anschreiben): extract shared runner, wire into UI server, fix stale test assertions
**Was:** `lib/anschreiben-runner.ts` extrahiert den Anschreiben-Generierungs-Loop aus `scripts/run-anschreiben.ts`, damit `scripts/ui-server.ts` ihn über `POST /api/anschreiben` + `GET /api/anschreiben/status` wiederverwenden kann statt ihn zu duplizieren. `lib/anschreiben.ts` persistiert den `generated`-Status jetzt via `storage.update()` statt `storage.save(job)` (Merge auf aktuellen Diskstand statt Überschreiben — Generierung dauert 3-4 Min, in denen ein Browser-Edit passieren kann). Drei Tests in `test/anschreiben.test.ts` prüften noch das dadurch nicht mehr mutierte lokale `job`-Objekt statt den Storage-Stand — korrigiert.
**Nicht gebaut:** kein Frontend-UI (Button/Polling) für den neuen Endpoint — `ui/app.tsx` hat nur die State-Typen, keine Bedienelemente.
